# ============================================
# ENTORNO DE TESTS - JEST
# ============================================

PORT=3000
NODE_ENV=test
LOG_DIR=logs

# Base de datos - SEPARADA para no afectar desarrollo
DB_HOST=localhost
DB_PORT=5432
DB_NAME=event_system_test
DB_USER=postgres
DB_PASSWORD=postgres

# JWT - Secreto específico para tests
JWT_SECRET=test-jwt-secret-do-not-use-in-production

# SMTP - No necesario para tests
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME="EVENTIFY Test"
SMTP_FROM_EMAIL=test@eventify.com

# URLs
PLATFORM_TERMS_URL=http://localhost:3000/terms
PLATFORM_PRIVACY_URL=http://localhost:3000/privacy