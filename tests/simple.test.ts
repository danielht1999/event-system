// Prueba simple sin imports
describe('Pruebas básicas', () => {
  test('La suma funciona', () => {
    expect(1 + 1).toBe(2);
  });

  test('Los booleanos funcionan', () => {
    expect(true).toBe(true);
  });
});
